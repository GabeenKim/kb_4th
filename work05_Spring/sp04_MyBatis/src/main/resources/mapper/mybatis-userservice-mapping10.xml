<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="UserMapper10">	

	<!-- 결과값을 명시적으로 지정하면 select태그 -> 이때 컬럼명과 필드명이 다른 경우 as 안 쓰도록 설정 -->
	<resultMap type="user" id="userSelectMap">
		<result property="userId" column="user_id"/>
		<result property="userName" column="user_name"/>
		<result property="password" column="password"/>
		<result property="age" column="age"/>
		<result property="grade" column="grade"/>
		<result property="regDate" column="reg_date"/>
	</resultMap>
	
	<!-- sql 구문의 모듈화 :: 자주 사용하는 쿼리문은(select에서 컬럼명, insert에서 컬럼면) 따로 모듈화 해준다.
		 sql, include-->
	<sql id="select-users">
		SELECT user_id, user_name, password, age, grade, reg_date
		FROM users
	</sql>
	
	<sql id="orderdy-userid-desc">
		ORDER BY user_id DESC
	</sql>
	
	<!-- 정적 sql -->
	<select id="getUser" parameterType="string" resultMap="userSelectMap">
		<include refid="select-users" />
		WHERE password = #{VALUE}
	</select>
	<!-- 동적 sql -->
	<select id="getUserList" parameterType="user" resultMap="userSelectMap">
		<include refid="select-users" />
		<where>
			<if test="userName != null">
				user_name LIKE #{userName}
			</if>
			<if test="age != null">
				OR age = #{age}
			</if>
			
		</where>
		<include refid="orderdy-userid-desc"></include>
	</select>
	
	<!-- 추가 :: addUser, updateUser, removeUser -->
	<insert id="addUser"  parameterType="user" >
		INSERT 
		INTO users(user_id, user_name, password, age, grade, reg_date)
		VALUES(
			#{userId},
			#{userName},
			#{password},
			#{age},
			#{grade},
			#{regDate}		
		)
	</insert>
	
	<update id="updateUser" parameterType="user">
		UPDATE users
		SET user_name = #{userName}
		WHERE user_id = #{userId}
	</update>
	
	<delete id="removeUser" parameterType="string">
		DELETE users
		WHERE user_id = #{VALUE}
	</delete>
	
	
</mapper>