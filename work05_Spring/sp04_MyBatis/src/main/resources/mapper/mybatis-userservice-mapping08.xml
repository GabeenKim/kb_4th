<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="UserMapper08">	

	<!-- 결과값을 명시적으로 지정하면 select태그 -> 이때 컬럼명과 필드명이 다른 경우 as 안 쓰도록 설정 -->
	<resultMap type="user" id="userSelectMap">
		<result property="userId" column="user_id"/>
		<result property="userName" column="user_name"/>
		<result property="password" column="password"/>
		<result property="age" column="age"/>
		<result property="grade" column="grade"/>
		<result property="regDate" column="reg_date"/>
	</resultMap>
	
	<!-- sql 구문의 모듈화 :: 자주 사용하는 쿼리문은(select에서 컬럼명, insert에서 컬럼면) 따로 모듈화 해준다.
		 sql, include-->
	<sql id="select-users">
		SELECT user_id, user_name, password, age, grade, reg_date
		FROM users
	</sql>
	
	<sql id="orderdy-userid-desc">
		ORDER BY user_id DESC
	</sql>
	
	
	<!-- 정적 쿼리문 -->
	<select id="getUserList01" parameterType="user" resultMap="userSelectMap">
		<include refid="select-users" />
		WHERE user_id = #{userId}
	</select>
	
	<!--동적인 쿼리문(only select 문에서만):: where 절 조건이 달라져도 하나의 select문으로 여러가지 조건을 한번에 처리함. 
		ex) userName ='홍길동' and age = '30'인 사람을 검색하는 경우 
			userName ='홍길동'검색하는 경우 
			age = '30'인 사람을 검색하는 경우 
			모든 사람 검색 경우 
		-> 정적쿼리는 4개가 나옴. 동적인 쿼리 사용하면 시간 단축. 효율성 증대
	-->
	<select id="getUserList" parameterType="user" resultMap="userSelectMap">
		<include refid="select-users" />
		<where>
			<if test="userName != null">
				user_name LIKE #{userName}
			</if>
			<if test="age != null">
				OR age = #{age}
			</if>
			
		</where>
		<include refid="orderdy-userid-desc"></include>
	</select>
	
	
	
	
	
</mapper>