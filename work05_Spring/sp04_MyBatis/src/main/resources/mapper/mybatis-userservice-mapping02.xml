<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="UserMapper02">
	<!--회원의 아이디가 user인 사람을 검색하는 쿼리 || user객체에 집어 넣어야 하니까 AS 필요-->
	<select id="getUser01" parameterType="user" resultType="user" >
		SELECT user_id AS userId, user_name AS userName, password, age, grade, reg_date AS redDate
		FROM users
		WHERE user_id LIKE #{VALUE}
		ORDER BY userId 
	</select>	
	
	<select id="getUser02" parameterType="user" resultType="user" >
		SELECT user_id AS userId, user_name AS userName, password, age, grade, reg_date AS redDate
		FROM users
		WHERE user_id LIKE #{userId}
	</select>
		
	<!-- user로 시작하는 id를 갖는 사람을 검색...like 연산자와 함께 %와일드 카드를 함께 사용. -->
	<select id="getUser03" parameterType="string" resultType="user" >
		SELECT user_id AS userId, user_name AS userName, password, age, grade, reg_date AS redDate
		FROM users
		WHERE user_id LIKE #{VALUE}|| '%'
	</select>
	
	<!-- 아이디가 01로 끝나는 사람을 검색. like 연산자와 함께 %와일드 카드를 함께 사용 -->
	<select id="getUser04" parameterType="user" resultType="user" >
		SELECT user_id AS userId, user_name AS userName, password, age, grade, reg_date AS redDate
		FROM users
		WHERE user_id LIKE '%'|| #{userId}
	</select>
</mapper>